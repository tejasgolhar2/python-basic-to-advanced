# USE OF COUNT OPERATOR

# The count operator in Tuple is used to count the number of occurrences of an element within it

repeat = (1,1,2,2,3,3,333,4,1,3)
one_occurence = repeat.count(1)

print(one_occurence)