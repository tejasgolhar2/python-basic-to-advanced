name = input("Enter a string: ")
length = len(name)
print(f"There are {length} number of characters in the entered string.")

#NOTE:      The len() function counts all characters, 
#       including whitespace characters, in the given object when determining the length.